CREATE TABLE proyecto_Colaboradores (
    id SERIAL PRIMARY KEY,
    proyecto_id INT REFERENCES proyectos(id),
    usuario_id INT REFERENCES usuario(id) UNIQUE,
    activo BOOLEAN DEFAULT TRUE
);









































































