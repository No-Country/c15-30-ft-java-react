CREATE TABLE stackUsuario (
    id SERIAL PRIMARY KEY,
    usuario_id INT REFERENCES usuario(id),
    tecnologia_id INT REFERENCES tecnologias(id),
    activo BOOLEAN DEFAULT TRUE
);








































































