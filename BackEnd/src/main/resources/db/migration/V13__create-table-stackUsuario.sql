CREATE TABLE stack_usuario (
    id SERIAL PRIMARY KEY,
    usuario_id INT REFERENCES usuario(id),
    tecnologia_id INT REFERENCES tecnologias(id) UNIQUE,
    activo BOOLEAN DEFAULT TRUE
);








































































