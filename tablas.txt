CREATE TABLE Paises (
    Id SERIAL PRIMARY KEY,
    Nombre VARCHAR NOT NULL
);

CREATE TABLE Paises (
    Id SERIAL PRIMARY KEY,
    Nombre VARCHAR NOT NULL
);

CREATE TABLE Status (
    Id SERIAL PRIMARY KEY,
    Nombre VARCHAR NOT NULL
);

CREATE TABLE Dificultades (
    Id SERIAL PRIMARY KEY,
    Nombre VARCHAR NOT NULL
);

CREATE TABLE USUARIO (

)